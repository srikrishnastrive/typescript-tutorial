interface complexNumber {
    real : number,
    imaginary : number
}

interface complexNumber {
    add : (num : complexNumber)=> complexNumber;
}

/**
 * interface complexNumber {
    real : number,
    imaginary : number
    add : (num : complexNumber)=> complexNumber;
}
 * 
 */